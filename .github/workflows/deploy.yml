name: Deploy and Run Script

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to Render
        run: |
          render deploy srv-cqukrvogph6c73ffujpg

      - name: Run additional script
        run: |
          python main.py
          
